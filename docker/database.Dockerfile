FROM postgres:15-alpine

# Placeholder for custom database initialization scripts or configurations
# e.g., installing pgvector extension dependencies if compiled locally
# COPY init.sql /docker-entrypoint-initdb.d/
